{
  "scripts": {
    "build": "cd frontend && rm -rf node_modules && npm install --legacy-peer-deps && DISABLE_ESLINT_PLUGIN=true npm run build",
    "start": "cd frontend && npm start"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "react-markdown": "^10.1.0"
  }
}
